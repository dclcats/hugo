name: github pages

on:
  push:
    branches:
      - master  # Set a branch to deploy

jobs:
  deploy:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true  # Fetch Hugo themes (true OR recursive)
          fetch-depth: 0    # Fetch all history for .GitInfo and .Lastmod

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'
          extended: true
      
      - name: Gitalk secret edit
        env:
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        run: ./setGitalkSecret.sh
        working-directory: ./config/_default
        shell: bash

      - name: Build
        run: hugo --minify -e github

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
          PUBLISH_BRANCH: gh-pages # default
          publish_dir: ./public
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'

      # - id: test-env 
      #   run: echo "::set-output name=test_id::123345"


      # - name: Create commit comment
      #   uses: peter-evans/commit-comment@v1
      #   with:
      #     body: |
      #       This is a multi-line test comment
      #       - With GitHub **Markdown**
      #       - Created by [commit-comment][1]
      #       - Test id -- ${{steps.test-env.outputs.test_id}}

      #       [1]: https://github.com/peter-evans/commit-comment